future possibility :
- wireless synchronize by local network
- offline synchronize via USB. user can connect devices to USB (phone / DAP / external drive), read as file transfer mode. app can select a folder on that device, then synchronize the media files inside between desktop library and device folder library.

dev notes :
all intended 1.0.0 features has been developed at this points.

fixing v1.1 :
- [x] select all function based on filtered result. At the moments it select all items regardless of the filters. (FIXED)