offline synchronize via USB :
user can connect devices to USB (phone / DAP / external drive), read as file transfer mode.
app can select a folder on that device, then synchronize the media files inside between desktop library and device folder library.

future possibility :
wireless synchronize by local network


must have feature :
- playlist manager to manage music
- playlist watcher, any playlist from youtube can be added, and on the list, add 2 button to apply watcher on audio, video, or both. need scheduler for interval watching. watcher will download audio/video/both if new list is added. the logic is for yt-dlp to download the playlist list first, check if there is new, then if there is new ones, then download that. so system doesnt have to redownload whole list and take out that already existed.
- about page!
- versioning and updating to check on github release.
- API for future development possibility.
 
fixing :
- app icon!
